import random
def rand():
    count = 0
    c=0
    list = []
    list2 = []
    list3 = []
    while count < 10:
        numbers = int(input("enter your numbers\n"))
        list.append(numbers)
        count+=1

    while c<5:
        random_number = random.choice(list)
        list2.append(list.index(random_number))
        c+=1
   

    list2.sort()
    for item in list2:
        rand_5numbers = list[item]
        list3.append(rand_5numbers)

    return list3


print(rand())
